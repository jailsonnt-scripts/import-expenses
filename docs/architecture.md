This document will be updated as the system evolves.

The architecture is being developed incrementally using an AI-assisted workflow, prioritizing correctness, simplicity, and testability before introducing abstractions.

## Current Direction

Input CSV/OFX → Source detection → Parsing → Validation → Normalization → Output CSV
